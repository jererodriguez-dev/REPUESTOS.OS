# Documentación interna del proyecto (no publicar)
CONTEXTO-JM.md
HISTORIAL.md
sql-corregido.sql

# Dump del schema real de la base (no publicar)
schema-actual.sql

# Migraciones SQL (las corre Jere en Supabase; no se publican con el sitio)
migracion-operaciones.sql

# Harness de pruebas en seco del importador (no es parte del sitio)
harness-cat.mjs
